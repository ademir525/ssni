# Mangofy API Credentials
MANGOFY_API_KEY=sua_api_key_aqui
MANGOFY_STORE_CODE=seu_store_code_aqui

# Configuração do Servidor
PORT=3000

# URLs permitidas para CORS (separar por vírgula)
ALLOWED_ORIGINS=http://localhost:5173,https://seu-dominio.com

# Webhook URL (opcional - para receber notificações de pagamento)
WEBHOOK_URL=https://seu-dominio.com/api/webhook

# Configuração do Banco de Dados MySQL
DB_HOST=xcgc0k8kc888ogs8ow4kw8o8
DB_USER=pag_user
DB_PASSWORD=Mariadb@2026
DB_NAME=pagamentos

# Tempo de expiração do PIX em minutos (default: 30)
PIX_EXPIRATION_MINUTES=30

# Telefone padrão para pagamentos (quando não informado)
DEFAULT_PHONE=11999999999

# =====================================================
# UTMify - Rastreamento de Conversões
# =====================================================
# URL do webhook UTMify para enviar conversões com UTMs
# Obtenha esta URL no painel da UTMify
UTMIFY_WEBHOOK_URL=https://api.utmify.com.br/webhook/seu-token-aqui
